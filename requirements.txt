# TTS API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
httpx>=0.26.0

# Audio preprocessing
# NOTE: requires Python 3.10 (GPT-SoVITS constraint). Use conda env below.
librosa>=0.10.2
soundfile>=0.12.3
numpy>=1.26.0

# Video to audio helper (URL download)
yt-dlp>=2024.1.0
